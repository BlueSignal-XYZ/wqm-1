"""
Third-party system integrations.

Each sub-package talks TO an external system the customer already owns (a
smart breaker, a panel, ...). None of them make the WQM-1 *be* that system:
the unit stays a water-quality monitor whose relays are a local interlock.
"""
