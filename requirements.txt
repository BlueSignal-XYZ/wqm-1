# Core firmware dependencies — universal across host boards (Raspberry Pi,
# Arduino UNO Q / VENTUNO Q, generic Linux). Direct-header hardware drivers
# live in requirements-rpi.txt — setup.sh installs that file only on
# Raspberry Pi hosts. See docs/platforms.md.
pyserial==3.5
pycryptodome==3.23.0
pyyaml==6.0.3
flask==3.1.3
qrcode==8.2
